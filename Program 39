#Print a right-angled triangle star pattern using a for loop.

n = int(input("Enter the number of rows for the triangle: "))
for i in range(1, n + 1):  # Loop through each row
    print('*' * i)  # Print i stars for the ith row
    
