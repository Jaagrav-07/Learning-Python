#Reverse a number.

num = int(input("Enter an integer to reverse: ")) # Get integer input from the user
original_num = num # Store the original number
reversed_num = 0 # variable to store the reversed number
while num > 0: # Loop runs as long as nos is greater than 0
    digit = num % 10 # Get the last digit of the number
    reversed_num = reversed_num * 10 + digit # Add the digit to the reversed number
    num //= 10 # Remove the last digit from the original number
