#Program 4: Sum of First N Natural Numbers
#Task:
#Compute the sum of the first N natural numbers using a loop.

N = int(input("Enter an integer N: "))
current_sum = 0  # Sum counter initialized to 0
for i in range(1, N + 1): # Loop from 1 to N (included)
    current_sum += i  #Add number i to the total till loop completes

print(f"The sum of the first {N} natural numbers is: {current_sum}")
