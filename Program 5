#Program 5: Reverse a Number
#Problem Statement:
#Reverse a given integer using a loop (e.g., 123 → 321).

num = int(input("Enter an integer: "))
reversed_num = 0
temp_num = abs(num)  
while temp_num > 0:
    digit = temp_num % 10
    reversed_num = reversed_num * 10 + digit
    temp_num //= 10
if num < 0:
    reversed_num = -reversed_num
print(f"The reverse of {num} is: {reversed_num}")
