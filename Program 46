#Check whether a number is a palindrome.

num = int(input("Enter an integer: "))
original_num = num
reversed_num = 0
while num > 0:  #Starts a loop that continues until every digit of the number has been processed.
    digit = num % 10
    reversed_num = reversed_num * 10 + digit
    num //= 10
if original_num == reversed_num: #Compares the saved original nos with the new reversed number
    print(f"{original_num} is a palindrome.")
else:
    print(f"{original_num} is not a palindrome.")
