#Take a number as input and print whether it is an integer or a float.

num_input = input("Enter a number: ")

# Try to convert to float
num_float = float(num_input)

# Check if the float value is an integer using is_integer()
if num_float.is_integer():
    print(f"The number {num_input} is an integer.")
else:
    print(f"The number {num_input} is a float.")
